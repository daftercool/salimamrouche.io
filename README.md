# Portfolio Website

This workspace currently contains the built application assets only:

- `index.html`
- `assets/index-D4LyFDrM.js` (minified React bundle)
- `assets/index-CiyAgzpd.css`

## Notes

- `assets/index-D4LyFDrM.js` is a compiled/minified bundle and is not intended for direct editing.
- For a more organized development workflow, keep source code in a separate `src/` directory and generate assets into `assets/`.
- If you want, I can help scaffold a clean source structure for this portfolio site using React, Vite, or plain HTML/JS/CSS.
